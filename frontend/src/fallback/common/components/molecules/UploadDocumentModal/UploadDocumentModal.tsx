import { FC } from "react";

interface Props {
  isOpen: boolean;
  onClose: () => void;
  employeeId: number;
  parentId: number;
}

const UploadDocumentModal: FC<Props> = () => <></>;

export default UploadDocumentModal;
