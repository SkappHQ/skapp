import { SettingsTab } from "~community/settings/types/SettingsTabTypes";

export const getEnterpriseSettingsTabs = (
  translateText: (keys: string[]) => string
): SettingsTab[] => {
  return [];
};
