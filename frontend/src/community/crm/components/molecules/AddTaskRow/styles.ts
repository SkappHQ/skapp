const styles = {
  // wrapper suppresses Card's default outline so it blends into the task list border
  wrapper: "[&>div>div]:outline-none! [&>div>div]:rounded-none",
  border: "border-t border-[#e5e7eb]"
};

export default styles;
