const styles = () => ({
  iconStyles: {
    display: "inline-flex",
    width: "fit-content",
    alignItems: "center",
    gap: "0.5rem",
    backgroundColor: "var(--color-tertiary-background)",
    borderRadius: "9.375rem",
    padding: "0.5rem 1rem"
  }
});

export default styles;
